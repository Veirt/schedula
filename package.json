{
  "name": "schedula",
  "private": true,
  "scripts": {
    "dev:client": "bun --cwd client dev",
    "dev:server": "bun --cwd server dev",
    "dev:docker": "docker compose -f docker-compose.dev.yml up -d --build",
    "notifier": "source notifier/venv/bin/activate && python notifier/main.py"
  },
  "workspaces": [
    "client",
    "server"
  ]
}
