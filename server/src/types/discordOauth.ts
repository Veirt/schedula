export type DiscordUserData = {
    id: string
    username: string
    avatar: string
    discriminator: string
    global_name: string
    accent_color: string
    banner_color: string
}

export type DiscordAuthData = {
    access_token: string
    refresh_token: string
}
