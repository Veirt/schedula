// uno.config.ts
import { defineConfig } from "unocss"

export default defineConfig({
    theme: {
        colors: {
            primary: "#151515",
            secondary: "#343434",
        },
    },
})
